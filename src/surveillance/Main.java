package surveillance;

import ui.PreviewFrame;
import javafx.application.Application;

public class Main {
    public static void main(String[] args) {
        Application.launch(PreviewFrame.class, args);
    }
}
